Time series classification dataset format in matlab

dataset --- [1 x dataset_size] struct array with fields:
  ts    --- time series: [num_components x ts_length] double
  label --- class label: integer in 1:num_classes
